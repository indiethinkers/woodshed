# Optional development-only fallbacks. Prefer entering keys in Settings so
# release builds store them in the operating-system credential store.
DEEPGRAM_API_KEY=
WOODSHED_AGENT_API_KEY=

# Optional single-account Gmail development fallback. Normal app accounts are
# configured in Settings and their App Passwords are stored by the OS.
GMAIL_EMAIL=
GMAIL_APP_PASSWORD=
