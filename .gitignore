# dependencies
node_modules/

# build output
dist/
build/

# local environment
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# coverage
coverage/

# typescript
*.tsbuildinfo

# editor and OS files
.DS_Store
.idea/
.vscode/

# Tauri / Rust build artifacts
src-tauri/target/
src-tauri/gen/schemas/

# OAuth keys / local secrets
.env.tauri
*.p12
*.pfx
*.mobileprovision

.gstack/

# Per-user Claude Code settings (do not commit)
.claude/settings.local.json

# TanStack Router auto-generated route tree
src/routeTree.gen.ts

# TanStack build/dev cache
.tanstack/

# Agent tooling & installed skills (reproducible via `bunx skills add`) —
# machine/agent-specific, not project artifacts.
.agents/
.claude/skills/
.claude/worktrees/
.hermes/
skills-lock.json

# Live working tracker written by the `improve` skill. Anchored to the repo
# root so similarly named documentation directories remain trackable.
/plans/
